name: lab_multi
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.5
  google_fonts: ^6.2.1
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  cupertino_icons: ^1.0.8
  audioplayers: ^6.1.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/dices/
    - assets/images/balls/
    - assets/xylophone/note1.wav
    - assets/xylophone/note2.wav
    - assets/xylophone/note3.wav
    - assets/xylophone/note4.wav
    - assets/xylophone/note5.wav
    - assets/xylophone/note6.wav
    - assets/xylophone/note7.wav